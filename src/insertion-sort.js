// Implement insertion sort in an iterative way following a TDD approach.

// This first test helps to know that the unit test framework is working.
export function playground(arr) {
  return 0;
}

export function step_0(arr) {
  return arr;
}
